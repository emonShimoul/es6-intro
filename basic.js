
// variable
var deposit = 400;

// condition
if(deposit < 500){

}
else if (deposit == 500) {
    
} else {
    
}

// array
const numbers = [12,5,67,89,53,123,43];
const numberCount = numbers.length;
numbers.pop();
numbers.push(111);
numbers[2] = 555;
if(numbers.indexOf(222) != -1){

}

if(numbers.includes(222)){

}

// loop
// while, for
for(const number of numbers){

}

// function
function fullName(first, second){
    const name = first + ' ' + second;
}
const person = fullName('Emon', 'Shimul');

// object
const bottle = {color: 'yellow', contain: 'watera', price: '50'};